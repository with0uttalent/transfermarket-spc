PORT=3000
JWT_SECRET=change_this_to_a_long_random_secret_string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Telegram integration (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_LIVE_CHANNEL_ID=
# TELEGRAM_PROXY=socks5://host:port

# CORS origin for production (optional)
# ALLOWED_ORIGIN=https://example.com

# Docker deployments set these via the Dockerfile; only override for
# non-standard layouts. Outside Docker leave them unset (defaults:
# database/transfermarket.db and public/images/uploads).
# DB_PATH=/data/transfermarket.db
# UPLOADS_DIR=/data/uploads
